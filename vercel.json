{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build && npx rimraf .vercel/output",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
